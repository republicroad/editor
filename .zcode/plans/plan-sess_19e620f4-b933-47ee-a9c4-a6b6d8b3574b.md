## 目标

把内核会话的 S007 裁决依据（pinned 独立键 vs auto 升格的四层理由）转录沉淀进文档，作为保留策略治理维度设计的权威记录。仅改文档 + 一笔提交。

## 写入点（三处）

1. **`docs/libsuggest/S007-version-pin.md` 追记节**（锚点已读取，第 45-50 行）：在既有追记后追加「裁决依据：为何不复用 auto 升格」小节，完整转录四层理由——
   ① auto（来源标记）与 pinned（保留意图）回答不同问题，复用丢来源信息且自动存档冒充手动；
   ② 升格不可逆，unpin 无从得知是否翻回 auto（宿主旧 PATCH {auto:false} workaround 正踩此点）；
   ③ 治理谓词保持正交（`auto && !versionName && !pinned` 三维合取，命名豁免在 64 批已是先例）；
   ④ 写入者与写入时机不同（保存路径 vs 面板事后 updateVersionMeta，PATCH 契约不含 auto）；
   结语：三正交维度各自标记组合表达，升格方案一比特硬编码交集、丢信息且不可逆。
2. **`docs/18-legacy-graph-compatibility.md` §5 存储治理**：保留策略契约行更新为四维豁免口径
   （manual 永久；auto 滚动+按日检查点；**命名版本与钉住版本豁免**——`auto && !versionName && !pinned`
   正交合取，引 S007 裁决依据），总览表 ④ 行同步。
3. **`docs/03` 第七十批记录**：裁决行扩充一句（「内核裁决 pinned 为独立正交维度——来源/命名/保留
   三键组合表达，未复用 auto 升格，理由全文见 libsuggest/S007 追记」）。

## 提交

一笔 docs 提交到 `reui`，不 push。无代码变更。